riority_queue in the similar fasion like other contianers beacuse these are not containeres these are the wrapper on the container know as adapter this restrict the some function like iteraiting and direxct acces

    // for(auto it : st){